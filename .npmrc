# Project-level npm configuration for ChartSentinel
# All npm operations will use D drive for caching and global packages

cache=D:\npm-cache
prefix=D:\npm-global

# Ensure reproducible installs
legacy-peer-deps=true
